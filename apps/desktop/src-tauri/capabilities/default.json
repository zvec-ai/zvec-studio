{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Zvec Studio desktop shell. Only the minimum surface area required to run the embedded webview is exposed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
